% {{GUIDE_NAME}} — Main Document
% Generated by /new-guide skill
% Compile: lualatex -shell-escape {{MAIN}}.tex

\documentclass[a4paper,11pt,oneside]{scrbook}

% ── Shared preamble (from hub) ──────────────────────────────────────────────
\usepackage{shared/interview-preamble-tufte}
\usepackage{shared/interview-variants}
\usepackage{shared/los-macros}

% ── Project-specific extensions ─────────────────────────────────────────────
\usepackage{{{EXTENSIONS_STY}}}

% ── Guide version (override shared preamble's \providecommand) ─────────────
\renewcommand{\guideversion}{1.0}

% ── Bibliography ────────────────────────────────────────────────────────────
\addbibresource{references.bib}

% ── Document metadata ──────────────────────────────────────────────────────
\title{{{GUIDE_NAME}}}
\author{Brandon Behring}
\date{{{DATE}}}

\begin{document}

\frontmatter
\maketitle
\tableofcontents

\mainmatter

% ── Chapters ────────────────────────────────────────────────────────────────
% \include{chapters/ch01}
% \include{chapters/ch02}
% Add chapters as created...

% ── Appendices ──────────────────────────────────────────────────────────────
\appendix
% \include{appendices/A_quick_reference}
% \include{appendices/B_gotchas}
% \include{appendices/C_glossary}
% \include{appendices/D_interview_prep}

\backmatter
\printbibliography[heading=bibintoc]

\end{document}
