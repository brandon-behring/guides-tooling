"""Generators for derived guide artifacts (stub dashboards, …).

Second-cut module: produces the regenerable derived files that the Bronze/Silver
auditors expect to find but that are intentionally gitignored (so they never
travel in a carve and are rebuilt on demand in the consuming repo).
"""
